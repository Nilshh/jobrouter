services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: job-scraper
    ports:
      - "8080:5000"  # ← Ändere 5000 zu 8080 (oder einen anderen freien Port)
    volumes:
      - .:/app
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped
    environment:
      - FLASK_ENV=production
      - PYTHONUNBUFFERED=1
    networks:
      - job-scraper-network

networks:
  job-scraper-network:
    driver: bridge
